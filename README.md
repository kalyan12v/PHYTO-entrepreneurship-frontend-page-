after setting up the file in your editing tool such as "vs code" open terminal and set up the "venv" by using the command "venv/scripts/activate"

afterthat use the command "flask --app app.py run" to run the code on flask
